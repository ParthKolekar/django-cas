============
Installation
============

At the command line::

    $ easy_install django-cas

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cas
    $ pip install django-cas
